cmake_minimum_required(VERSION 3.18)
project(pocketpal_onnxruntime_spike LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
add_library(pocketpal_onnxruntime_spike STATIC src/onnx_session.cpp)
target_include_directories(pocketpal_onnxruntime_spike PUBLIC include)
enable_testing()
add_executable(onnx_session_test tests/onnx_session_test.cpp)
target_link_libraries(onnx_session_test PRIVATE pocketpal_onnxruntime_spike)
add_test(NAME onnx_session_fixture COMMAND onnx_session_test ${CMAKE_CURRENT_SOURCE_DIR}/../../fixtures/onnx/identity_float.onnx)
